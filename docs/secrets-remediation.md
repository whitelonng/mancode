# Secrets 独立审核修复记录

修复基线：`08560b5aa27e6e5203b67a19d1d6ed3cdd8f1eee`。本轮只处理独立审核发现的六项问题，保持网关退役与 Secrets V1 的既有功能范围。原交付任务的完成记录是历史记录，不能替代这次针对性复验。

后续修复任务：`local:01M2SZ4S3BF01FSSY2FCEEARW3`。本机 macOS arm64、Node 25.9.0；所有测试使用合成数据。

| 发现 | 修复与当前状态 |
|---|---|
| IMP-1 | 已修复。人工批准摘要从实际准备安装的 spec 读取，显示入口、凭据槽位到别名的映射及非系统运行库身份。修改入口或映射时，摘要必须改变。 |
| IMP-2 | 已修复。每次运行创建包快照之外的私有 TMPDIR；正常成功、失败、超时、取消和启动失败后清理。连续调用不再因非敏感临时文件导致包校验失败。SIGKILL/主机崩溃不保证执行清理。 |
| IMP-3 | 已修复。按文件当前大小进行有界读取，额外探测增长并拒绝，返回紧凑 Buffer；16 个小文件不再保留约 256 MiB 底层容量。测试检查保留容量，不把它表述为实测 RSS。 |
| EVD-1 | 已补齐进程中断替代验收。六个写入阶段各执行 EIO 故障注入与真实 SIGKILL，用全新进程验证完整旧版或新版、秘密和批准 revision 一致、缓存重建。物理断电及文件系统硬件故障仍未验证。 |
| EVD-2 | 部分完成。新增原生 Entry 构造、getSecret、setSecret 异常测试，通过真实 MacKeyProvider 和 CLI 验证固定错误、无明文兜底、无业务启动。真实 Keychain 锁定及用户拒绝仍为 `manual_test_required`，不能以这些 mock 或缺失密钥试验替代。 |
| EVD-3 | 已修复门禁。要求 host 退出 0、每次恰好一条认证请求、runner audit 新增唯一成功事件、各调用 runId 不同。直接调用还核对 stdout 回执与 audit。Agent 的自然语言回答不参与成功判断。 |

## 验证

- 三项实现修复：先在旧实现确认真实失败断言，再修复；原 58 项与新增 9 项合计 67/67 通过。
- 新增恢复测试 12/12、原生后端边界测试 5/5、宿主门禁测试 12/12 通过。独立复审再次运行四个新增测试文件，38/38 通过。
- 新构建的真实 Codex、Claude 探针退出 0：各 1 条认证请求、各 1 条独立 runner 成功事件。加上直接调用共 3 请求、3 次认证、3 个不同 runId；删除测试密钥后 exit 5 且零新增请求。证据在用户指定测试目录的 `gateway-retirement-secrets-20260918/phase-e-remediation-hosts/`。
- 全量 `npm run check` 退出 0，包含 lint、typecheck、build、dist、依赖 high 门禁及覆盖率测试：187 个文件，1746 项通过、1 项原有真实升级用例因未配置专用环境跳过。没有 Secrets 跳过。

恢复测试不在生产代码加入故障开关。隔离子进程在临时文件部分写入、文件同步后、rename 前、rename 后、目录同步前、registry 写入时注入故障或等待真实终止。锁恢复使用真实存活/死亡 PID；测试只推进读进程的时钟以覆盖租约到期，分别断言活进程不可抢锁、死进程未到期不可绕过、到期可回收。

保留的失败记录：首次复现有 Vitest ESM spy 设置问题；首次恢复测试有 jsonc-parser 打包格式问题，均修正测试工具后重验。一次沙箱内旧 HTTP 回归遭本地监听 EPERM，正常工具审批后同一测试命令通过。这些不计为产品断言修复，也不抹去失败历史。

## 尚未完成的环境验收

真实锁定/用户拒绝需要专用 macOS 账户或隔离测试机器，并能通过正常系统交互控制该测试账户的默认钥匙串。本次后端绑定默认系统目标，不提供独立钥匙串路径；没有修改用户日常钥匙串、默认钥匙串或全局搜索列表。

在专用环境中，需要分别保留锁定和拒绝授权前后的实际操作、CLI 固定输出/退出码、业务请求为零、保险箱未修改及无明文回退的证据。还需观察系统授权提示期间的响应和取消行为；组件 timeout 测试不能证明该同步原生调用的交互行为。

未运行远端 Node 22/24、Windows/Linux CI；不声称全部 S03 或所有平台已验收通过。本轮验证未发布 npm 包或使用生产凭据。
